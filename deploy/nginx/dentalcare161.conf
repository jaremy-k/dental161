upstream dentalcare_app {
    server 127.0.0.1:3000;
    keepalive 32;
}

# Пока нет сертификата — только HTTP (nginx -t пройдёт).
server {
    listen 80;
    listen [::]:80;
    server_name dentalcare161.ru www.dentalcare161.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://dentalcare_app;
        proxy_http_version 1.1;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host  $host;
        proxy_set_header Connection        "";

        proxy_connect_timeout 10s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
        proxy_buffering off;
    }
}

# После certbot — заменить server :80 на редирект и раскомментировать блоки ниже.
#
# sudo mkdir -p /var/www/certbot
# sudo certbot certonly --webroot -w /var/www/certbot \
#   -d dentalcare161.ru -d www.dentalcare161.ru

# server {
#     listen 80;
#     listen [::]:80;
#     server_name dentalcare161.ru www.dentalcare161.ru;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://dentalcare161.ru$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     http2 on;
#     server_name www.dentalcare161.ru;
#
#     ssl_certificate     /etc/letsencrypt/live/dentalcare161.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dentalcare161.ru/privkey.pem;
#
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_session_tickets off;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers off;
#
#     return 301 https://dentalcare161.ru$request_uri;
# }
#
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     http2 on;
#     server_name dentalcare161.ru;
#
#     ssl_certificate     /etc/letsencrypt/live/dentalcare161.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dentalcare161.ru/privkey.pem;
#
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_session_tickets off;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers off;
#
#     access_log /var/log/nginx/dentalcare161.access.log;
#     error_log  /var/log/nginx/dentalcare161.error.log;
#
#     client_max_body_size 10m;
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;
#     gzip_min_length 256;
#
#     location / {
#         proxy_pass http://dentalcare_app;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host  $host;
#         proxy_set_header Connection        "";
#
#         proxy_connect_timeout 10s;
#         proxy_send_timeout    60s;
#         proxy_read_timeout    60s;
#         proxy_buffering off;
#     }
#
#     location /_next/static/ {
#         proxy_pass http://dentalcare_app;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }
